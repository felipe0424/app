---
import { Image } from 'astro:assets';
import Layout from '../layouts/Layout.astro';
import bgHero from '../assets/bg-1.png';
import logoPrincipal from '../assets/logo-principal.svg';
import image1 from '../assets/image1.png';
import image2 from '../assets/image2.png';
import fabiano from '../assets/image3.png';
import image4 from '../assets/image4.png';
import image5 from '../assets/image5.png';
import elemento from '../assets/elements/elemento-1.svg';
import arrow from '../assets/icons/arrow.svg';
import fire from '../assets/icons/fire.svg';
import scroll from '../assets/icons/screll.svg';

const menuItems = ['Início', 'Especialidades', 'Cursos', 'Avaliações', 'Sobre', 'Contatos'];

const featureCards = [
  {
    title: 'Lorem ipsum dolor sit amet',
    text: 'Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.',
  },
  {
    title: 'Lorem ipsum dolor sit amet',
    text: 'Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.',
  },
  {
    title: 'Lorem ipsum dolor sit amet',
    text: 'Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.',
  },
  {
    title: 'Lorem ipsum dolor sit amet',
    text: 'Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.',
  },
];

const resultCards = [
  { image: fabiano, label: 'franca.beta' },
  { image: image1, label: 'nutrifabianomoura' },
  { image: image4, label: 'clinica fabiano moura' },
  { image: image5, label: 'vanicleia.p' },
  { image: image2, label: 'resultado' },
  { image: image4, label: 'depoimento' },
];

const accordionItems = [
  {
    title: 'Lorem ipsum dolor sit amet consectetur',
    text: 'Lorem ipsum dolor sit amet consectetur. Porta nibh ultricies feugiat tincidunt donec purus. Egestas malesuada urna cursus venenatis elementum gravida ac tellus feugiat.',
  },
  {
    title: 'Lorem ipsum dolor sit amet consectetur',
    text: 'Nisl velit faucibus adipiscing nec non nulla viverra. Ornare ut vel turpis quam. Sit purus neque sed sit amet pellentesque a ligula.',
  },
  {
    title: 'Lorem ipsum dolor sit amet consectetur',
    text: 'Felis nulla id leo cras turpis. Urna viverra est dolor mi convallis aliquam. Pharetra tristique libero et in molestie faucibus aenean.',
  },
  {
    title: 'Lorem ipsum dolor sit amet consectetur',
    text: 'Faucibus lacus mauris viverra eget. Sed quam et ullamcorper neque sit sit augue condimentum risus.',
  },
  {
    title: 'Lorem ipsum dolor sit amet consectetur',
    text: 'Lectus fermentum neque sed tortor aliquet eget dictumst sed. Nulla ut congue tempor ultrices cursus.',
  },
];
---

<Layout title="Fabiano Moura | Excelência Nutricional">
  <main class="home-page text-text bg-background w-full max-w-full box-border justify-center items-center overflow-hidden">
    <section id="inicio" class="hero-shell">
      <Image
        src={bgHero}
        alt=""
        loading="eager"
        class="hero-bg"
      />

      <header class="site-header">
        <a href="#inicio" class="brand-link" aria-label="Fabiano Moura Excelência Nutricional">
          <Image src={logoPrincipal} alt="Fabiano Moura Excelência Nutricional" loading="eager" class="brand-logo" />
        </a>

        <nav class="site-nav" aria-label="Navegação principal">
          {menuItems.map((item) => (
            <a href={`#${item.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '')}`}>
              {item}
            </a>
          ))}
        </nav>

        <a
          href="/login"
          class="private-access"
        >
          <span>Acesso Exclusivo</span>
          <Image src={fire} alt="" width={24} height={24} loading="eager" class="private-icon" />
        </a>
      </header>

      <div class="hero-content">
        <div class="hero-copy">
          <p class="hero-kicker">
            Você está em busca da
          </p>

          <h1 class="hero-title">
            Excelência
            <span class="block">Nutricional?</span>
          </h1>

          <p class="hero-text">
            Lorem ipsum dolor sit amet consectetur adipisicing elit. Magnam quam repellat vero non mollitia cum est voluptates. Lorem ipsum dolor sit amet consectetur adipisicing elit. Magnam quam repellat vero non mollitia cum est voluptates. Lorem ipsum dolor sit amet consectetur adipisicing elit. Magnam quam repellat vero non mollitia cum est voluptates. Lorem ipsum dolor sit amet consectetur adipisicing elit. Magnam quam repellat vero non mollitia cum est voluptates.
          </p>

          <a
            href="#especialidades"
            class="hero-cta"
          >
            <span>Loren Ipsum</span>
            <Image src={arrow} alt="" width={24} height={24} loading="eager" class="hero-cta-icon" />
          </a>
        </div>

        <div class="hero-visual">
          <Image
            src={fabiano}
            alt="Fabiano Moura"
            loading="eager"
            class="fabiano-photo"
          />

          <Image
            src={elemento}
            alt=""
            loading="eager"
            class="hero-element"
          />
        </div>
      </div>

      <div class="scroll-cue">
        <span>Science in Sports Performance</span>
        <Image src={scroll} alt="" width={22} height={27} loading="eager" class="scroll-icon" />
      </div>
    </section>

    <section id="especialidades" class="landing-fold feature-fold">
      <div class="cream-board">
        <div class="section-heading heading-navy">
          <h2>Lorem ipsum dolor sit amet consectetur</h2>
          <p>Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.</p>
        </div>

        <div class="feature-grid">
          {featureCards.map((card) => (
            <article class="feature-card">
              <div class="feature-card-title">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M12 3.2l2.6 5.27 5.82.85-4.21 4.1.99 5.8L12 16.48 6.8 19.22l.99-5.8-4.21-4.1 5.82-.85L12 3.2Z" />
                </svg>
                <h3>{card.title}</h3>
              </div>
              <p>{card.text}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="landing-fold split-fold split-fold-cream" id="sobre">
      <div class="split-copy">
        <p class="section-kicker">Lorem ipsum</p>
        <h2>Lorem ipsum dolor sit amet consectetur.</h2>
        <p>
          Lorem ipsum dolor sit amet consectetur. Sed quam et ullamcorper neque sit sit augue condimentum risus. Lectus fermentum neque sed tortor aliquet eget dictumst sed. Nulla ut congue tempor ultrices cursus.
        </p>
        <a href="#contatos" class="section-cta">
          <span>Loren Ipsum dolor sit</span>
          <Image src={arrow} alt="" width={24} height={24} loading="lazy" class="section-cta-icon" />
        </a>
      </div>

      <div class="image4-stage">
        <Image src={image4} alt="Fabiano Moura" loading="lazy" class="image4-photo" />
      </div>
    </section>

    <section id="avaliacoes" class="landing-fold results-fold">
      <div class="section-heading heading-navy">
        <h2>Lorem ipsum dolor sit amet consectetur.</h2>
        <p>Lorem ipsum dolor sit amet consectetur. Sed quam et ullamcorper neque sit sit augue condimentum risus.</p>
      </div>

      <div class="results-row" aria-label="Avaliações e resultados">
        {resultCards.map((card) => (
          <article class="result-card">
            <Image src={card.image} alt={card.label} loading="lazy" class="result-image" />
          </article>
        ))}
      </div>

      <div class="slider-actions" aria-hidden="true">
        <button type="button">←</button>
        <button type="button">→</button>
      </div>
    </section>

    <section class="landing-fold centered-cta-fold">
      <div class="centered-copy">
        <p>Lorem ipsum dolor sit amet consectetur</p>
        <h2>Lorem ipsum dolor sit amet consectetur</h2>
        <p>
          Lorem ipsum dolor sit amet consectetur. Turpis lectus egestas quam dictum est risus. Commodo sed accumsan ut velit nunc sed vitae quam dolor.
        </p>
        <a href="#contatos" class="section-cta">
          <span>Loren Ipsum dolor sit</span>
          <Image src={arrow} alt="" width={24} height={24} loading="lazy" class="section-cta-icon" />
        </a>
      </div>
    </section>

    <section id="cursos" class="landing-fold feature-fold feature-fold-compact">
      <div class="cream-board">
        <div class="section-heading heading-navy">
          <h2>Lorem ipsum dolor sit amet consectetur</h2>
          <p>Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.</p>
        </div>

        <div class="feature-grid">
          {featureCards.map((card) => (
            <article class="feature-card">
              <div class="feature-card-title">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                  <path d="M12 3.2l2.6 5.27 5.82.85-4.21 4.1.99 5.8L12 16.48 6.8 19.22l.99-5.8-4.21-4.1 5.82-.85L12 3.2Z" />
                </svg>
                <h3>{card.title}</h3>
              </div>
              <p>{card.text}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <section class="landing-fold accordion-fold">
      <div class="giant-title" aria-hidden="true">
        <span>Lorem</span>
        <strong>Lorem ipsum dolor</strong>
      </div>

      <div class="accordion-list">
        {accordionItems.map((item, index) => (
          <details class="accordion-item" open={index === 0}>
            <summary>
              <span>{item.title}</span>
              <span class="accordion-arrow">▼</span>
            </summary>
            <p>{item.text}</p>
          </details>
        ))}
      </div>
    </section>

    <section class="landing-fold split-fold image5-fold">
      <div class="image5-card">
        <Image src={image5} alt="Fabiano Moura em ensaio fotográfico" loading="lazy" class="image5-photo" />
      </div>

      <div class="split-copy">
        <p class="section-kicker">Lorem ipsum</p>
        <h2>Lorem ipsum dolor sit amet consectetur.</h2>
        <p>
          Lorem ipsum dolor sit amet consectetur. Sed quam et ullamcorper neque sit sit augue condimentum risus. Lectus fermentum neque sed tortor aliquet eget dictumst sed. Nulla ut congue tempor ultrices cursus.
        </p>
        <a href="#contatos" class="section-cta">
          <span>Loren Ipsum dolor sit</span>
          <Image src={arrow} alt="" width={24} height={24} loading="lazy" class="section-cta-icon" />
        </a>
      </div>
    </section>

    <section id="contatos" class="contact-fold">
      <div class="contact-copy">
        <h2>Lorem ipsum dolor sit amet</h2>
        <p>Lorem ipsum dolor sit amet consectetur. Egestas in dui dictumst duis malesuada platea turpis rhoncus.</p>
      </div>

      <div class="contact-actions">
        <a href="https://api.whatsapp.com/send?phone=5587988674049&amp;text=Ol%C3%A1,%20gostaria%20de%20saber%20mais!." target="_blank" rel="noopener" class="contact-card">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.82 19.82 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6A19.82 19.82 0 0 1 2.12 4.18 2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.13.96.35 1.9.66 2.8a2 2 0 0 1-.45 2.11L8.05 9.9a16 16 0 0 0 6.05 6.05l1.27-1.27a2 2 0 0 1 2.11-.45c.9.31 1.84.53 2.8.66A2 2 0 0 1 22 16.92Z" />
          </svg>
          <span>
            <strong>Lorem ipsum dolor</strong>
            <small>Lorem ipsum dolor sit amet consectetur.</small>
          </span>
        </a>

        <a href="mailto:contato@fabianomoura.com" class="contact-card">
          <svg viewBox="0 0 24 24" aria-hidden="true">
            <path d="m22 2-7 20-4-9-9-4 20-7Z" />
            <path d="M22 2 11 13" />
          </svg>
          <span>
            <strong>Lorem ipsum dolor</strong>
            <small>Lorem ipsum dolor sit amet consectetur.</small>
          </span>
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
  .home-page {
    --page-max: 1480px;
    --page-gutter: clamp(24px, 5.2vw, 104px);

    display: flex;
    width: 100%;
    max-width: 100%;
    box-sizing: border-box;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    min-height: 100vh;
    overflow-x: hidden;
    background: #00183e;
    color: #e4d694;
    font-family: "Inter", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  .hero-shell {
    position: relative;
    width: 100%;
    min-height: 100svh;
    overflow: hidden;
    isolation: isolate;
  }

  .hero-shell::after {
    position: absolute;
    inset: 0;
    z-index: -1;
    content: "";
    background: linear-gradient(90deg, rgba(0, 24, 62, 0.14), rgba(0, 24, 62, 0) 46%, rgba(0, 24, 62, 0.1));
    pointer-events: none;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: -2;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .site-header {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    z-index: 40;
    display: grid;
    grid-template-columns: minmax(260px, 420px) minmax(520px, 1fr) minmax(190px, 235px);
    align-items: center;
    gap: 28px;
    width: min(calc(100% - (var(--page-gutter) * 2)), var(--page-max));
    margin: 0 auto;
    padding: 34px 0 0;
  }

  .brand-link,
  .brand-logo {
    display: block;
    width: 100%;
  }

  .site-nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 38px;
    color: #e4d694;
    font-size: 1.15rem;
    font-weight: 600;
    line-height: 1;
    white-space: nowrap;
  }

  .site-nav a {
    color: inherit;
    text-decoration: none;
    transition: color 160ms ease;
  }

  .site-nav a:hover {
    color: #fff8c7;
  }

  .private-access,
  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #00183e;
    font-weight: 800;
    text-decoration: none;
    background: linear-gradient(90deg, #d0b362 0%, #fffbd0 100%);
    border-radius: 8px;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.14);
    transition: background 160ms ease;
  }

  .private-access:hover,
  .hero-cta:hover {
    background: linear-gradient(90deg, #dbc777 0%, #fffce0 100%);
  }

  .private-access {
    justify-self: end;
    min-width: 220px;
    min-height: 44px;
    gap: 12px;
    padding: 0 22px;
    font-size: 1rem;
  }

  .private-icon {
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
  }

  .hero-content {
    position: absolute;
    inset: 0;
    z-index: 20;
    display: grid;
    grid-template-columns: minmax(520px, 0.82fr) minmax(560px, 1fr);
    align-items: center;
    gap: 52px;
    width: min(calc(100% - (var(--page-gutter) * 2)), var(--page-max));
    margin: 0 auto;
    padding: 118px 0 92px;
  }

  .hero-copy {
    width: 100%;
    max-width: 760px;
    transform: translateY(28px);
  }

  .hero-kicker {
    margin: 0 0 18px;
    font-size: 1.65rem;
    font-weight: 400;
    line-height: 1.1;
  }

  .hero-title {
    max-width: 690px;
    margin: 0;
    font-size: 5.8rem;
    font-weight: 800;
    line-height: 0.9;
  }

  .hero-title span {
    display: block;
  }

  .hero-text {
    max-width: 820px;
    margin: 32px 0 0;
    color: #eee5a6;
    font-size: 1.45rem;
    font-weight: 400;
    line-height: 1.25;
  }

  .hero-cta {
    min-width: 225px;
    min-height: 46px;
    gap: 30px;
    margin-top: 52px;
    padding: 0 32px;
    font-size: 1rem;
  }

  .hero-cta-icon {
    width: 22px;
    height: 22px;
    flex: 0 0 auto;
  }

  .hero-visual {
    position: relative;
    align-self: stretch;
    min-height: 0;
    pointer-events: none;
  }

  .fabiano-photo {
    position: absolute;
    right: 1vw;
    bottom: 0;
    z-index: 20;
    width: auto;
    height: 76svh;
    max-width: none;
    object-fit: contain;
    filter: drop-shadow(0 24px 34px rgba(0, 0, 0, 0.26));
  }

  .hero-element {
    position: absolute;
    right: -1.5vw;
    bottom: 9svh;
    z-index: 30;
    width: 18rem;
    height: auto;
    opacity: 0.38;
    filter: blur(1.5px);
    transform: rotate(10deg);
  }

  .scroll-cue {
    position: absolute;
    right: 0;
    bottom: 34px;
    left: 0;
    z-index: 35;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
    color: #e4d694;
    font-size: 1.05rem;
    line-height: 1;
    pointer-events: none;
  }

  .scroll-icon {
    width: 22px;
    height: 27px;
  }

  .landing-fold {
    position: relative;
    width: 100%;
    box-sizing: border-box;
    min-height: 100svh;
    overflow: hidden;
    background: #00183e;
  }

  .section-heading {
    max-width: 880px;
    margin: 0 auto;
    text-align: center;
  }

  .section-heading h2,
  .centered-copy h2,
  .split-copy h2,
  .contact-copy h2 {
    margin: 0;
    font-size: 4rem;
    font-weight: 800;
    line-height: 1.05;
  }

  .section-heading p {
    max-width: 760px;
    margin: 26px auto 0;
    font-size: 1.45rem;
    line-height: 1.25;
  }

  .heading-navy {
    color: #00183e;
  }

  .feature-fold {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: clamp(72px, 7vw, 112px) var(--page-gutter);
  }

  .cream-board {
    width: 100%;
    max-width: var(--page-max);
    padding: clamp(56px, 5vw, 76px) clamp(32px, 7vw, 128px) clamp(70px, 6vw, 96px);
    background: #eee5a6;
    border: 3px solid rgba(0, 24, 62, 0.16);
    border-radius: 8px;
    color: #00183e;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 44px 56px;
    max-width: 1040px;
    margin: 62px auto 0;
  }

  .feature-card {
    min-height: 178px;
    padding: 26px 28px;
    border: 2px solid rgba(0, 24, 62, 0.35);
    border-radius: 8px;
  }

  .feature-card-title {
    display: flex;
    align-items: center;
    gap: 22px;
    padding-bottom: 18px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.85);
  }

  .feature-card svg {
    width: 32px;
    height: 32px;
    flex: 0 0 auto;
  }

  .feature-card svg path {
    fill: none;
    stroke: #00183e;
    stroke-linejoin: round;
    stroke-width: 2;
  }

  .feature-card h3 {
    margin: 0;
    font-size: 1.45rem;
    font-weight: 800;
    line-height: 1.1;
  }

  .feature-card p {
    margin: 18px 0 0;
    font-size: 1.35rem;
    line-height: 1.22;
  }

  .split-fold {
    display: grid;
    grid-template-columns: minmax(420px, 560px) minmax(520px, 680px);
    align-items: center;
    justify-content: center;
    gap: clamp(48px, 5vw, 84px);
    padding: clamp(78px, 7vw, 112px) var(--page-gutter);
    color: #ffffff;
    background: #020407;
  }

  .split-fold-cream {
    grid-template-columns: minmax(420px, 540px) minmax(620px, 820px);
    overflow: hidden;
    background: #00183e;
  }

  .split-copy {
    position: relative;
    z-index: 10;
    max-width: 720px;
  }

  .section-kicker {
    margin: 0 0 38px;
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
  }

  .split-copy h2 {
    color: #ffffff;
  }

  .split-copy p:not(.section-kicker) {
    margin: 38px 0 0;
    color: rgba(255, 255, 255, 0.92);
    font-size: 1.55rem;
    line-height: 1.38;
  }

  .section-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 340px;
    min-height: 56px;
    gap: 22px;
    margin-top: 42px;
    padding: 0 32px;
    color: #00183e;
    font-size: 1.35rem;
    font-weight: 700;
    text-decoration: none;
    background: linear-gradient(90deg, #d0b362 0%, #fffbd0 100%);
    border-radius: 8px;
  }

  .section-cta-icon {
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
  }

  .image4-stage {
    position: relative;
    min-height: 78svh;
    margin-right: clamp(-96px, -4vw, -36px);
    overflow: visible;
    background: #00183e;
  }

  .image4-photo {
    position: absolute;
    bottom: clamp(-88px, -6svh, -44px);
    left: clamp(-54px, -2.8vw, -20px);
    width: min(74vw, 1380px);
    max-width: none;
    height: auto;
    transform: none;
    object-fit: contain;
    object-position: center;
  }

  .split-fold-cream .image4-photo {
    /* mix-blend-mode: lighten; */
    opacity: 1;
    /*filter: saturate(1.03) contrast(1.02);*/
  }

  .results-fold {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: clamp(72px, 7vw, 104px) 0 clamp(64px, 6vw, 92px);
    color: #00183e;
    background: #eee5a6;
  }

  .results-row {
    display: grid;
    grid-auto-columns: clamp(240px, 18vw, 340px);
    grid-auto-flow: column;
    gap: 24px;
    width: min(calc(100% - (var(--page-gutter) * 2)), var(--page-max));
    margin-top: 58px;
    margin-right: auto;
    margin-left: auto;
    overflow-x: auto;
    padding: 0 0 12px;
    scroll-snap-type: x proximity;
  }

  .result-card {
    height: 500px;
    overflow: hidden;
    background: rgba(0, 24, 62, 0.08);
    border-radius: 8px;
    scroll-snap-align: center;
  }

  .result-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .slider-actions {
    display: flex;
    justify-content: center;
    gap: 28px;
    margin-top: 36px;
  }

  .slider-actions button {
    width: 54px;
    height: 54px;
    color: #eee5a6;
    font-size: 2rem;
    font-weight: 800;
    line-height: 1;
    background: #00183e;
    border: 0;
    border-radius: 999px;
  }

  .centered-cta-fold {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 82svh;
    padding: clamp(82px, 7vw, 112px) var(--page-gutter);
    color: #ffffff;
    background: #020407;
  }

  .centered-copy {
    max-width: 1160px;
    text-align: center;
  }

  .centered-copy p:first-child {
    margin: 0 0 30px;
    font-size: 1.8rem;
    font-weight: 600;
  }

  .centered-copy h2 {
    max-width: 920px;
    margin: 0 auto;
    font-size: 4.6rem;
    color: #ffffff;
  }

  .centered-copy p:not(:first-child) {
    max-width: 1260px;
    margin: 46px auto 0;
    font-size: 1.75rem;
    font-weight: 600;
    line-height: 1.45;
  }

  .feature-fold-compact {
    min-height: 92svh;
  }

  .accordion-fold {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 120svh;
    padding: clamp(74px, 7vw, 110px) var(--page-gutter);
    color: #eee5a6;
    background: #020407;
  }

  .giant-title {
    position: relative;
    width: min(100%, var(--page-max));
    min-height: 310px;
    display: grid;
    place-items: center;
    text-align: center;
    color: #e4d694;
  }

  .giant-title span {
    font-size: 10rem;
    font-weight: 900;
    line-height: 0.85;
  }

  .giant-title strong {
    position: absolute;
    bottom: 55px;
    font-size: 2.6rem;
    font-weight: 900;
    line-height: 1;
  }

  .accordion-list {
    width: min(100%, var(--page-max));
  }

  .accordion-item {
    color: #00183e;
    background: #e4d694;
    border-top: 14px solid #020407;
    overflow: hidden;
  }

  .accordion-item:first-child {
    border-top: 0;
    border-radius: 8px 8px 0 0;
  }

  .accordion-item:last-child {
    border-radius: 0 0 8px 8px;
  }

  .accordion-item summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    min-height: 86px;
    padding: 0 56px;
    cursor: pointer;
    list-style: none;
    font-size: 2rem;
    font-weight: 900;
  }

  .accordion-item summary::-webkit-details-marker {
    display: none;
  }

  .accordion-arrow {
    flex: 0 0 auto;
    font-size: 2.2rem;
  }

  .accordion-item[open] .accordion-arrow {
    transform: rotate(180deg);
  }

  .accordion-item p {
    margin: 0;
    padding: 0 56px 54px;
    color: #eee5a6;
    background: #020407;
    font-size: 2rem;
    line-height: 1.35;
  }

  .image5-fold {
    grid-template-columns: minmax(420px, 0.42fr) minmax(520px, 0.58fr);
    background: #020407;
  }

  .image5-card {
    overflow: hidden;
    border: 2px solid #e4d694;
    border-radius: 8px;
  }

  .image5-photo {
    display: block;
    width: 100%;
    height: min(74svh, 760px);
    object-fit: cover;
    object-position: center;
  }

  .contact-fold {
    display: grid;
    grid-template-columns: minmax(300px, 420px) minmax(640px, 900px);
    align-items: center;
    justify-content: center;
    gap: clamp(44px, 5vw, 76px);
    width: 100%;
    box-sizing: border-box;
    min-height: 46svh;
    padding: clamp(64px, 6vw, 92px) var(--page-gutter);
    color: #ffffff;
    background: #020407;
  }

  .contact-copy h2 {
    max-width: 560px;
    color: #ffffff;
    font-size: 3rem;
  }

  .contact-copy p {
    max-width: 580px;
    margin: 28px 0 0;
    font-size: 1.7rem;
    line-height: 1.25;
  }

  .contact-actions {
    display: grid;
    grid-template-columns: repeat(2, minmax(260px, 1fr));
    gap: 42px;
  }

  .contact-card {
    display: grid;
    grid-template-columns: 128px 1fr;
    align-items: center;
    min-height: 210px;
    gap: 34px;
    padding: 38px 46px;
    color: #00183e;
    text-decoration: none;
    background: #e4d694;
    border-radius: 8px;
  }

  .contact-card svg {
    width: 96px;
    height: 96px;
  }

  .contact-card svg path {
    fill: none;
    stroke: #00183e;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
  }

  .contact-card strong {
    display: block;
    font-size: 2rem;
    font-weight: 900;
    line-height: 1.05;
  }

  .contact-card small {
    display: block;
    margin-top: 16px;
    font-size: 1.75rem;
    line-height: 1.2;
  }

  @media (min-width: 1800px) {
    .home-page {
      --page-max: 1540px;
      --page-gutter: clamp(36px, 6vw, 132px);
    }

    .site-nav {
      gap: 46px;
      font-size: 1.25rem;
    }

    .hero-title {
      font-size: 6.35rem;
    }

    .hero-text {
      font-size: 1.58rem;
    }
  }

  @media (max-width: 1390px) {
    .site-header {
      grid-template-columns: minmax(240px, 345px) minmax(420px, 1fr) minmax(178px, 210px);
      gap: 22px;
    }

    .site-nav {
      gap: 26px;
      font-size: 0.95rem;
    }

    .private-access {
      min-width: 198px;
      min-height: 40px;
      font-size: 0.9rem;
    }

    .hero-content {
      grid-template-columns: minmax(470px, 0.88fr) minmax(450px, 1fr);
      gap: 30px;
    }

    .hero-kicker {
      font-size: 1.35rem;
    }

    .hero-title {
      font-size: 4.8rem;
    }

    .hero-text {
      max-width: 670px;
      font-size: 1.16rem;
    }

    .fabiano-photo {
      height: 70svh;
    }

    .hero-element {
      width: 14rem;
    }

    .section-heading h2,
    .split-copy h2,
    .contact-copy h2 {
      font-size: 3.15rem;
    }

    .section-heading p {
      font-size: 1.2rem;
    }

    .cream-board {
      padding: 58px 6vw 72px;
    }

    .feature-grid {
      gap: 34px;
      max-width: 940px;
    }

    .feature-card h3 {
      font-size: 1.2rem;
    }

    .feature-card p {
      font-size: 1.12rem;
    }

    .split-fold {
      grid-template-columns: minmax(390px, 520px) minmax(440px, 620px);
      gap: 44px;
    }

    .section-kicker {
      font-size: 1.6rem;
      margin-bottom: 26px;
    }

    .split-copy p:not(.section-kicker) {
      font-size: 1.22rem;
    }

    .section-cta {
      min-width: 300px;
      min-height: 52px;
      font-size: 1.12rem;
    }

    .result-card {
      height: 420px;
    }

    .centered-copy h2 {
      font-size: 3.7rem;
    }

    .centered-copy p:not(:first-child) {
      font-size: 1.38rem;
    }

    .giant-title span {
      font-size: 7.4rem;
    }

    .accordion-item summary {
      padding: 0 40px;
      font-size: 1.55rem;
    }

    .accordion-item p {
      padding: 0 40px 42px;
      font-size: 1.45rem;
    }

    .contact-fold {
      grid-template-columns: 1fr;
    }

    .contact-card {
      grid-template-columns: 86px 1fr;
      min-height: 168px;
    }

    .contact-card svg {
      width: 74px;
      height: 74px;
    }

    .contact-card strong {
      font-size: 1.55rem;
    }

    .contact-card small {
      font-size: 1.28rem;
    }
  }

  @media (max-width: 1180px) {
    .site-header {
      position: relative;
      display: flex;
      width: auto;
      padding: 24px 24px 0;
    }

    .brand-link {
      width: 270px;
      max-width: 70vw;
    }

    .site-nav,
    .private-access,
    .scroll-cue {
      display: none;
    }

    .hero-content {
      position: relative;
      display: block;
      width: auto;
      min-height: calc(100svh - 88px);
      padding: 72px 24px 0;
    }

    .hero-copy {
      max-width: 620px;
      transform: none;
    }

    .hero-kicker {
      font-size: 1.18rem;
    }

    .hero-title {
      max-width: 540px;
      font-size: 3.4rem;
      line-height: 0.95;
    }

    .hero-text {
      max-width: 620px;
      font-size: 1rem;
      line-height: 1.35;
    }

    .hero-cta {
      min-width: 204px;
      min-height: 44px;
      margin-top: 34px;
    }

    .hero-visual {
      position: absolute;
      right: -90px;
      bottom: 0;
      width: 470px;
      height: 48svh;
      opacity: 0.65;
    }

    .fabiano-photo {
      right: 0;
      height: 100%;
    }

    .hero-element {
      display: none;
    }

    .landing-fold {
      min-height: auto;
    }

    .feature-fold,
    .centered-cta-fold,
    .split-fold,
    .contact-fold {
      padding: 72px 24px;
    }

    .cream-board {
      padding: 44px 24px 54px;
    }

    .section-heading h2,
    .centered-copy h2,
    .split-copy h2,
    .contact-copy h2 {
      font-size: 2.5rem;
      line-height: 1.08;
    }

    .section-heading p,
    .centered-copy p:not(:first-child),
    .split-copy p:not(.section-kicker),
    .contact-copy p {
      font-size: 1.05rem;
      line-height: 1.42;
    }

    .feature-grid {
      grid-template-columns: 1fr;
      gap: 22px;
      margin-top: 42px;
    }

    .feature-card {
      min-height: auto;
      padding: 22px;
    }

    .feature-card h3 {
      font-size: 1.05rem;
    }

    .feature-card p {
      font-size: 1rem;
    }

    .split-fold,
    .image5-fold {
      display: grid;
      grid-template-columns: 1fr;
      gap: 38px;
    }

    .image4-stage {
      min-height: min(58svh, 520px);
      margin: 0 -24px;
    }

    .image4-photo {
      bottom: -44px;
      left: 50%;
      width: min(112vw, 880px);
      transform: translateX(-50%);
      object-position: center;
    }

    .results-fold {
      padding: 68px 0;
    }

    .results-row {
      grid-auto-columns: minmax(230px, 290px);
      overflow-x: auto;
      padding: 0 24px 10px;
      scroll-snap-type: x mandatory;
    }

    .result-card {
      height: 380px;
      scroll-snap-align: center;
    }

    .slider-actions {
      display: none;
    }

    .centered-copy p:first-child,
    .section-kicker {
      font-size: 1.2rem;
    }

    .accordion-fold {
      min-height: auto;
      padding-top: 54px;
    }

    .giant-title {
      min-height: 190px;
    }

    .giant-title span {
      font-size: 5rem;
    }

    .giant-title strong {
      bottom: 42px;
      font-size: 1.4rem;
    }

    .accordion-item summary {
      min-height: 70px;
      padding: 0 24px;
      font-size: 1.08rem;
    }

    .accordion-arrow {
      font-size: 1.3rem;
    }

    .accordion-item p {
      padding: 0 24px 28px;
      font-size: 1rem;
    }

    .image5-photo {
      height: 520px;
    }

    .contact-actions {
      grid-template-columns: 1fr;
      gap: 22px;
    }

    .contact-card {
      grid-template-columns: 62px 1fr;
      gap: 22px;
      min-height: 138px;
      padding: 28px;
    }

    .contact-card svg {
      width: 56px;
      height: 56px;
    }

    .contact-card strong {
      font-size: 1.2rem;
    }

    .contact-card small {
      font-size: 1rem;
    }
  }

  @media (max-width: 560px) {
    .hero-title {
      font-size: 2.72rem;
    }

    .hero-text {
      font-size: 0.95rem;
    }

    .hero-visual {
      right: -170px;
      width: 430px;
      opacity: 0.42;
    }

    .feature-fold,
    .centered-cta-fold,
    .split-fold,
    .contact-fold {
      padding-right: 18px;
      padding-left: 18px;
    }

    .cream-board {
      padding: 34px 18px 42px;
    }

    .section-heading h2,
    .centered-copy h2,
    .split-copy h2,
    .contact-copy h2 {
      font-size: 2rem;
    }

    .section-cta {
      width: 100%;
      min-width: 0;
      font-size: 1rem;
    }

    .image4-stage {
      min-height: 320px;
    }

    .image4-photo {
      bottom: -30px;
      width: 130vw;
    }

    .result-card {
      height: 330px;
    }

    .giant-title span {
      font-size: 3.6rem;
    }

    .image5-photo {
      height: 420px;
    }
  }
</style>
